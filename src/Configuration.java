/**
 * Where WebServers configuration is specified.
 */
public abstract class Configuration {
    // The maximum number of concurrent threads in the thread pool.
    static final int THREAD_LIMIT = 10;
    // The server's name.
    static final String SERVER_NAME = "200010566's WebServer";
    // Name and location of the log file.
    static final String LOG_FILEPATH = "../log.txt";
}
